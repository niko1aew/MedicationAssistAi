# ============================================
# MedicationAssist - Environment Configuration
# ============================================
# Скопируйте этот файл в .env и настройте значения:
#   cp .env.example .env
#
# Как это работает:
# 1. Docker Compose читает .env автоматически
# 2. Переменные подставляются в docker-compose.yml
# 3. Они передаются в контейнер как environment variables
# 4. .NET переопределяет appsettings.json этими значениями
#
# Формат для .NET: секция__ключ (двойное подчёркивание)
#   ConnectionStrings__DefaultConnection -> ConnectionStrings.DefaultConnection
#   JwtSettings__Secret -> JwtSettings.Secret
# ============================================

# --------------------------------------------
# PostgreSQL Database
# --------------------------------------------
POSTGRES_DB=medicationassist
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_PORT=5432

# --------------------------------------------
# API Configuration  
# --------------------------------------------
# Внешний порт (доступ к API снаружи)
API_PORT=5000
# Внутренний порт контейнера
ASPNETCORE_HTTP_PORTS=8080

# --------------------------------------------
# JWT Authentication
# --------------------------------------------
# ВАЖНО: Секретный ключ должен быть минимум 32 символа!
# Для генерации: openssl rand -base64 32
JWT_SECRET=your_super_secret_key_at_least_32_characters_long_change_me!
JWT_ISSUER=MedicationAssist.API
JWT_AUDIENCE=MedicationAssist.Client
# Время жизни токена в минутах (60 = 1 час, 1440 = 24 часа)
JWT_EXPIRATION_MINUTES=60

# --------------------------------------------
# Telegram Bot Configuration
# --------------------------------------------
# Токен бота (получить у @BotFather)
TELEGRAM_BOT_TOKEN=123456789:ABCdef...
# Имя бота (без @)
TELEGRAM_BOT_USERNAME=YourBotUsername
# URL веб-сайта
WEBSITE_URL=https://medications.meteoassist.space

# --------------------------------------------
# Channel Subscription Requirement
# --------------------------------------------
# Username канала (без @) для проверки обязательной подписки
CHANNEL_USERNAME=meteoassist
# Полный URL канала для отображения пользователям
CHANNEL_URL=https://t.me/meteoassist