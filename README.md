# MedicationAssist - Система контроля приема лекарств

Информационная система для контроля приема лекарств с безопасной аутентификацией, персонализированным учетом, напоминаниями и Telegram ботом.

## 🚀 Описание

MedicationAssist - это REST API приложение с Telegram ботом, разработанное с использованием современных технологий и архитектурных практик:

- **.NET 9.0** - последняя версия платформы .NET
- **PostgreSQL 17** - надежная реляционная база данных
- **JWT Authentication** - безопасная аутентификация с токенами
- **BCrypt** - криптографически стойкое хэширование паролей
- **Swagger/OpenAPI** - интерактивная документация API с поддержкой JWT
- **Telegram Bot** - удобный интерфейс для управления лекарствами
- **Rate Limiting** - защита от перегрузки (100 req/min)
- **DDD (Domain-Driven Design)** - архитектурный подход
- **Rich Domain Model** - богатая доменная модель с бизнес-логикой
- **Entity Framework Core** - ORM для работы с базой данных
- **Serilog** - структурированное логирование
- **Docker** - контейнеризация приложения
- **xUnit, FluentAssertions, Moq** - тестирование

## ✨ Функциональность

### 🔐 Аутентификация и авторизация

- **Регистрация** пользователей с безопасным хэшированием паролей (BCrypt)
- **Вход в систему** с генерацией JWT токенов
- **Роли пользователей** (User, Admin)
- **Защита всех эндпоинтов** через JWT Bearer Authentication
- **Rate Limiting** для предотвращения злоупотреблений

### 👤 Управление пользователями

- Создание, обновление, удаление пользователей
- Получение информации о пользователе
- Управление ролями

### 💊 Управление лекарствами

- Создание списка лекарств для каждого пользователя
- Добавление информации о лекарстве (название, описание, дозировка)
- Редактирование и удаление лекарств

### 📋 Учет приема лекарств

- Регистрация факта приема лекарства
- Автоматическое проставление текущей даты/времени
- Просмотр истории приема с фильтрацией:
  - По датам (от/до)
  - По конкретному лекарству
- Редактирование и удаление записей о приеме

### ⏰ Напоминания о приеме

- Создание напоминаний с указанием времени и лекарства
- Автоматическая отправка уведомлений в Telegram
- Управление активностью напоминаний (включение/отключение)
- Поддержка множественных напоминаний для одного лекарства
- Учет часовых поясов пользователей

### 🌍 Поддержка часовых поясов

- Автоматическое определение часового пояса при регистрации
- Возможность изменения часового пояса в настройках
- Корректное отображение времени напоминаний и записей
- Хранение всех временных меток в UTC
- Поддержка IANA идентификаторов часовых поясов

### 🤖 Telegram бот

- **Регистрация и вход** через email/пароль или быстрый старт (Telegram ID)
- **Управление лекарствами** — добавление, просмотр, удаление
- **Запись приёмов** — быстрая фиксация с опциональными примечаниями
- **История приёмов** — просмотр за сегодня, неделю, месяц или всё время
- **Напоминания** — уведомления о времени приёма лекарств
- **Полностью на русском языке** с удобными inline-кнопками

## 📁 Структура проекта

```
MedicationAssist/
├── MedicationAssist.Domain/          # Доменный слой (сущности, интерфейсы)
│   ├── Common/                       # Базовые классы и исключения
│   ├── Entities/                     # Доменные сущности (User, Medication, etc.)
│   └── Repositories/                 # Интерфейсы репозиториев
├── MedicationAssist.Application/     # Слой приложения (use cases, DTOs)
│   ├── Common/                       # Общие классы (Result)
│   ├── DTOs/                         # Data Transfer Objects
│   └── Services/                     # Сервисы (AuthService, UserService, etc.)
├── MedicationAssist.Infrastructure/  # Инфраструктурный слой
│   ├── Data/                         # DbContext и конфигурации EF
│   ├── Repositories/                 # Реализации репозиториев
│   ├── Security/                     # JWT, BCrypt
│   └── Migrations/                   # Миграции EF Core
├── MedicationAssist.API/             # Web API
│   └── Controllers/                  # REST контроллеры (AuthController, etc.)
├── MedicationAssist.TelegramBot/     # Telegram бот
│   ├── Configuration/                # Настройки бота
│   ├── Handlers/                     # Обработчики команд и callback'ов
│   ├── Keyboards/                    # Inline-клавиатуры
│   ├── Resources/                    # Шаблоны сообщений (русский язык)
│   └── Services/                     # Сервисы (бот, сессии, напоминания)
└── MedicationAssist.Tests.Unit/      # Unit тесты
    ├── Domain/                       # Тесты доменной логики
    └── Application/                  # Тесты сервисов
```

## 🛠️ Требования

- [.NET 9.0 SDK](https://dotnet.microsoft.com/download/dotnet/9.0)
- [Docker Desktop](https://www.docker.com/products/docker-desktop/) (для запуска в контейнерах)
- PostgreSQL 17+ (если запуск без Docker)

## ⚙️ Конфигурация

Все секреты и порты вынесены в файл `.env`. Создайте его из шаблона:

```bash
cp .env.example .env
```

### Переменные окружения

#### Основные

| Переменная               | Описание                             | По умолчанию              |
| ------------------------ | ------------------------------------ | ------------------------- |
| `POSTGRES_DB`            | Имя базы данных                      | `medicationassist`        |
| `POSTGRES_USER`          | Пользователь БД                      | `postgres`                |
| `POSTGRES_PASSWORD`      | Пароль БД                            | `postgres`                |
| `POSTGRES_PORT`          | Порт PostgreSQL                      | `5432`                    |
| `API_PORT`               | Внешний порт API                     | `5000`                    |
| `ASPNETCORE_HTTP_PORTS`  | Внутренний порт контейнера           | `8080`                    |
| `JWT_SECRET`             | Секретный ключ JWT (мин. 32 символа) | —                         |
| `JWT_ISSUER`             | Издатель токенов                     | `MedicationAssist.API`    |
| `JWT_AUDIENCE`           | Аудитория токенов                    | `MedicationAssist.Client` |
| `JWT_EXPIRATION_MINUTES` | Время жизни токена (минуты)          | `60`                      |

#### Telegram бот

| Переменная                   | Описание                                           | Обязательная |
| ---------------------------- | -------------------------------------------------- | ------------ |
| `TELEGRAM_BOT_TOKEN`         | Токен бота от [@BotFather](https://t.me/BotFather) | ✅ Да        |
| `TELEGRAM_WEBHOOK_URL`       | URL для webhook (опционально)                      | Нет          |
| `DATABASE_CONNECTION_STRING` | Строка подключения к PostgreSQL                    | ✅ Да        |
| `JWT_SECRET_KEY`             | Секретный ключ JWT                                 | ✅ Да        |

> ⚠️ **Важно:** Файл `.env` добавлен в `.gitignore` и не должен коммититься в репозиторий.

## 🚀 Быстрый старт

### Вариант 1: Docker (Рекомендуется)

1. **Клонируйте репозиторий:**

```bash
git clone <repository-url>
cd MedicationAssist
```

2. **Создайте файл `.env` из примера:**

```bash
cp .env.example .env
```

3. **Настройте переменные окружения** в `.env` (при необходимости):

```env
# Database
POSTGRES_DB=medicationassist
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
POSTGRES_PORT=5432

# API
API_PORT=5000
ASPNETCORE_HTTP_PORTS=8080

# JWT
JWT_SECRET=YourSuperSecretKeyThatIsAtLeast32CharactersLong!
JWT_ISSUER=MedicationAssist.API
JWT_AUDIENCE=MedicationAssist.Client
JWT_EXPIRATION_MINUTES=60

# Telegram Bot (получите токен у @BotFather)
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

4. **Запустите приложение с помощью Docker Compose** (включая сервис миграций, который применяет схему перед стартом API и бота):

```bash
docker-compose up --build
```

5. **Доступ к приложению:**
   - API: `http://localhost:5000`
   - Swagger UI: `http://localhost:5000/swagger`
   - PostgreSQL: `localhost:5432`
   - Telegram бот: работает автоматически (если указан `TELEGRAM_BOT_TOKEN`)

### Вариант 2: Без Docker (и для Production-пайплайнов)

1. **Установите и запустите PostgreSQL**

2. **Создайте файл `.env`** из примера и настройте подключение:

```bash
cp .env.example .env
```

3. **Или обновите строку подключения** в `appsettings.json`:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=medicationassist;Username=postgres;Password=your_password"
  }
}
```

4. **Примените миграции базы данных вручную (рекомендуется выполнять через отдельный шаг деплоя/CI):**

```bash
# из корня репозитория
dotnet ef database update \
  --project MedicationAssist.Infrastructure \
  --startup-project MedicationAssist.API \
  --context ApplicationDbContext
```

5. **Запустите приложение:**

```bash
dotnet run
```

6. **Откройте Swagger UI:** `http://localhost:5000/swagger`

### Запуск Telegram бота (отдельно)

1. **Получите токен бота** у [@BotFather](https://t.me/BotFather) в Telegram

2. **Установите переменные окружения:**

```bash
# Windows (PowerShell)
$env:TELEGRAM_BOT_TOKEN="123456789:ABCdef..."
$env:DATABASE_CONNECTION_STRING="Host=localhost;Port=5432;Database=medicationassist;Username=postgres;Password=postgres"
$env:JWT_SECRET_KEY="SuperSecretKeyThatIsAtLeast32Chars"

# Linux/macOS
export TELEGRAM_BOT_TOKEN="123456789:ABCdef..."
export DATABASE_CONNECTION_STRING="Host=localhost;Port=5432;Database=medicationassist;Username=postgres;Password=postgres"
export JWT_SECRET_KEY="SuperSecretKeyThatIsAtLeast32Chars"
```

3. **Запустите бота:**

```bash
dotnet run --project MedicationAssist.TelegramBot
```

4. **Найдите бота** в Telegram и отправьте `/start`

## 📚 API Документация (Swagger UI)

### Доступ к Swagger

Откройте в браузере: **`http://localhost:5000/swagger`**

### Как использовать аутентификацию в Swagger:

1. **Зарегистрируйтесь** через `POST /api/auth/register`:

```json
{
  "name": "Иван Иванов",
  "email": "ivan@example.com",
  "password": "password123"
}
```

2. **Скопируйте полученный токен** из ответа (поле `token`)

3. **Нажмите кнопку "Authorize"** (🔒) в правом верхнем углу Swagger UI

4. **Введите токен** в формате: `Bearer {ваш_токен}`

5. **Нажмите "Authorize"** и закройте окно

6. Теперь все запросы будут отправляться с JWT токеном! 🎉

## 🤖 Telegram бот — Команды

| Команда        | Описание                            |
| -------------- | ----------------------------------- |
| `/start`       | Приветствие, меню регистрации/входа |
| `/menu`        | Главное меню с кнопками             |
| `/medications` | Список ваших лекарств               |
| `/add`         | Добавить новое лекарство            |
| `/intake`      | Записать приём лекарства            |
| `/history`     | История приёмов                     |
| `/reminders`   | Управление напоминаниями            |
| `/help`        | Показать справку                    |
| `/logout`      | Выйти из аккаунта                   |

### Возможности бота

- **Быстрый старт** — автоматическая регистрация через Telegram ID
- **Вход по email** — привязка к существующему аккаунту
- **Inline-кнопки** — удобная навигация без ввода команд
- **История по периодам** — сегодня, вчера, неделя, месяц, всё время
- **Напоминания** — уведомления в указанное время

## 🔌 API Эндпоинты

### 🔐 Authentication (Публичные)

- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему (получение JWT токена)

### 👤 Users (Требуется аутентификация)

- `GET /api/users` - Получить всех пользователей
- `GET /api/users/{id}` - Получить пользователя по ID
- `GET /api/users/by-email/{email}` - Получить пользователя по email
- `POST /api/users` - Создать пользователя (устарело, используйте `/api/auth/register`)
- `PUT /api/users/{id}` - Обновить пользователя
- `PUT /api/users/{id}/timezone` - Обновить часовой пояс пользователя
- `DELETE /api/users/{id}` - Удалить пользователя

### 💊 Medications (Требуется аутентификация)

- `GET /api/users/{userId}/medications` - Получить все лекарства пользователя
- `GET /api/users/{userId}/medications/{id}` - Получить лекарство по ID
- `POST /api/users/{userId}/medications` - Создать лекарство
- `PUT /api/users/{userId}/medications/{id}` - Обновить лекарство
- `DELETE /api/users/{userId}/medications/{id}` - Удалить лекарство

### 📋 Medication Intakes (Требуется аутентификация)

- `GET /api/users/{userId}/intakes` - Получить все приемы пользователя
  - Query параметры: `fromDate`, `toDate`, `medicationId`
- `GET /api/users/{userId}/intakes/{id}` - Получить прием по ID
- `POST /api/users/{userId}/intakes` - Зарегистрировать прием
- `PUT /api/users/{userId}/intakes/{id}` - Обновить прием
- `DELETE /api/users/{userId}/intakes/{id}` - Удалить прием

### ⏰ Reminders (Требуется аутентификация)

- `GET /api/users/{userId}/reminders` - Получить все напоминания пользователя
- `GET /api/users/{userId}/reminders/{id}` - Получить напоминание по ID
- `POST /api/users/{userId}/reminders` - Создать напоминание
- `PUT /api/users/{userId}/reminders/{id}` - Обновить напоминание
- `DELETE /api/users/{userId}/reminders/{id}` - Удалить напоминание
- `PATCH /api/users/{userId}/reminders/{id}/toggle` - Включить/отключить напоминание

## 💡 Примеры запросов

### Регистрация пользователя

```bash
curl -X POST http://localhost:5000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иван Иванов",
    "email": "ivan@example.com",
    "password": "securepass123",
    "timeZoneId": "Europe/Moscow"
  }'
```

**Ответ:**

```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
    "name": "Иван Иванов",
    "email": "ivan@example.com",
    "role": "User",
    "createdAt": "2024-12-02T10:00:00Z"
  }
}
```

### Вход в систему

```bash
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "ivan@example.com",
    "password": "securepass123"
  }'
```

### Создание лекарства (с токеном)

```bash
curl -X POST http://localhost:5000/api/users/{userId}/medications \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {your_jwt_token}" \
  -d '{
    "name": "Аспирин",
    "description": "Обезболивающее средство",
    "dosage": "500mg"
  }'
```

### Регистрация приема лекарства (с токеном)

```bash
curl -X POST http://localhost:5000/api/users/{userId}/intakes \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {your_jwt_token}" \
  -d '{
    "medicationId": "{medicationId}",
    "intakeTime": "2024-12-02T10:00:00Z",
    "notes": "Принято после завтрака"
  }'
```

## 🧪 Тестирование

Запуск всех тестов:

```bash
dotnet test
```

Запуск с покрытием кода:

```bash
dotnet test /p:CollectCoverage=true
```

**Текущее покрытие:** 46 тестов (Domain: 38, Application: 8)

## 📝 Логирование

Логи записываются в:

- **Консоль** (при запуске)
- **Файл:** `logs/medication-assist-YYYYMMDD.log`

Логи автоматически ротируются ежедневно, хранятся последние 30 дней.

**Уровни логирования:**

- `Debug` - детальная отладка
- `Information` - основные события (регистрация, вход, создание записей)
- `Warning` - нештатные ситуации
- `Error` - ошибки выполнения

## 🏗️ Архитектура

Проект следует принципам:

- **Clean Architecture** - разделение на слои с четкими границами
- **DDD** - доменная модель в центре архитектуры
- **Rich Domain Model** - бизнес-логика в доменных сущностях
- **SOLID** - принципы объектно-ориентированного проектирования
- **Repository Pattern** - абстракция доступа к данным
- **Unit of Work** - управление транзакциями
- **Options Pattern** - конфигурация через IOptions<T>

## 🔒 Безопасность

### Реализованные меры:

- ✅ **JWT аутентификация** (HS256)
- ✅ **BCrypt хэширование** паролей
- ✅ **Роли пользователей** (User, Admin)
- ✅ **Rate Limiting** (100 запросов/минуту)
- ✅ **[Authorize]** атрибут на всех защищенных контроллерах
- ✅ **Валидация** входных данных на всех уровнях
- ✅ **Telegram бот** — аутентификация через email/пароль или Telegram ID

### Рекомендации для Production:

- Используйте HTTPS (TLS 1.3+)
- Храните секреты в безопасном хранилище (Azure Key Vault, AWS Secrets Manager)
- **Никогда не коммитьте `.env` файл** — используйте `.env.example` как шаблон
- Настройте CORS политику под ваш домен
- Включите дополнительные меры защиты (2FA, Password Reset)

## 🛠️ Разработка

### Создание миграций

```bash
cd MedicationAssist.Infrastructure
dotnet ef migrations add MigrationName --startup-project ../MedicationAssist.API
```

### Применение миграций

```bash
dotnet ef database update --startup-project ../MedicationAssist.API
```

### Работа с Docker

```bash
# Запуск всех сервисов (postgres + миграции + API + бот)
docker-compose up --build

# Запуск только API и БД (без бота)
docker-compose up api postgres --build

# Запуск только Telegram бота
docker-compose up telegram-bot --build

# Остановка
docker-compose down

# Просмотр логов API
docker logs medicationassist-api -f

# Просмотр логов Telegram бота
docker logs medicationassist-telegram-bot -f

# Перезапуск
docker-compose restart

# Очистка (включая volumes)
docker-compose down -v
```

## 📄 Документация

- **[spec.md](spec.md)** - Полная техническая спецификация (версия 1.3)
- **[DEVELOPMENT.md](DEVELOPMENT.md)** - Руководство разработчика
- **Swagger UI** - Интерактивная документация API: `/swagger`
- **Telegram бот** - Отправьте `/help` боту для просмотра команд

## 📊 Версии

**Текущая версия:** 1.4

**История изменений:**

- **v1.4 (18.12.2025)** - Добавлена поддержка часовых поясов пользователей (IANA timezones), улучшена система напоминаний
- **v1.3 (15.12.2025)** - Добавлен Telegram бот (управление лекарствами, приёмы, напоминания, русский язык)
- **v1.2 (02.12.2025)** - Добавлена система аутентификации и авторизации (JWT, BCrypt, Rate Limiting)
- **v1.1 (01.12.2025)** - Добавлен Swagger UI
- **v1.0 (01.12.2025)** - Первый релиз

## 📜 Лицензия

MIT License

## 📧 Контакты

Для вопросов и предложений создайте issue в репозитории проекта.

---

**Made with ❤️ using .NET 9.0, Clean Architecture and Telegram.Bot**
